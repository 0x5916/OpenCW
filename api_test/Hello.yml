info:
  name: Hello
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/v1/hello
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
