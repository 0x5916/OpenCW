info:
  name: All Settings Get
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/v1/settings/all
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
