info:
  name: Update User Email
  type: http
  seq: 9

http:
  method: PUT
  url: http://localhost:8080/v1/user/email
  body:
    type: json
    data: |-
      {
        "email": "alan2@example.com"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
