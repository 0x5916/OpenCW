info:
  name: CW Settings Get
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/v1/settings/cw
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
