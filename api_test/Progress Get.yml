info:
  name: Progress Get
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/api/v1/cw/progress
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
