info:
  name: Update User Call Sign
  type: http
  seq: 11

http:
  method: PUT
  url: http://localhost:8080/v1/user/callsign
  body:
    type: json
    data: |-
      {
        "call_sign": "test"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
