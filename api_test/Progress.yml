info:
  name: Progress
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/api/v1/cw/progress
  body:
    type: json
    data: |-
      {
        "lesson": "1",
        "char_wpm": 25,
        "eff_wpm": 12,
        "accuracy": 0.8
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
